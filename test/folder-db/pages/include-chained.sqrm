
This is page-include-chained.

<%
  let info = db.find('info_box')
  let cities = db.find({ city: { $exists: true }})

  cities.forEach(city => {
    response.include(info,city.execute().json)
  })
%>
